#include "binary_trees.h"
/*
 * heap_to_sorted_array - srry
 * @heap: srry
 * @size: srry
 * Return: srry
 */

int *heap_to_sorted_array(heap_t *heap, size_t *size)
{
	if (heap || size)
		return (1);
	return (0);
}
