#include "binary_trees.h"

/*
 * array_to_heap - srry
 * @array: srry
 * @size: srry
 * Return: srry
 */

heap_t *array_to_heap(int *array, size_t size)
{
	heap_t *srry = NULL;

	if (array || size)
		return (srry);
	return (srry);
}
