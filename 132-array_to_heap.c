#include "binary_trees.h"

/*
 * array_to_heap - srry
 * @array: srry
 * @size: srry
 * Return: srry
 */

heap_t *array_to_heap(int *array, size_t size)
{
	if (array || size)
		return (1);
	return (0);
}
