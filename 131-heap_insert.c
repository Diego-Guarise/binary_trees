#include "binary_trees.h"

/**
 * heap_insert - srry
 * @root: srry
 * @value: srry
 * Return: srry/
 */

heap_t *heap_insert(heap_t **root, int value)
{
	if (root || value)
		return (*root);
	return (*root);
}
